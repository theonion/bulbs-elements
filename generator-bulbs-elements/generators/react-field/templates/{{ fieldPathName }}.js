const <%= fieldClassName %>Field = {
  initialState: {},
  actions: {
    initialAction: function (state, payload, store) {
      // write actual actions
      return state;
    },
  },
};

export default <%= fieldClassName %>Field;

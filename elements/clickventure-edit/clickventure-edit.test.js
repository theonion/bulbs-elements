import testElement from 'bulbs-elements/test/element';
import { assert } from 'chai';

testElement('<clickventure-edit>', function () {
  beforeEach(function () {
    this.element = this.renderElement({
      tag: 'clickventure-edit',
      props: {},
    });
  });

  it('demo', function () {
    debugger
  });
});
